layout()
    div(class="container w-full")
        div(class="mb-4")
            h1(class="text-xl font-bold") Inställningar
            
            div(class="mt-4")
                h3(class="text-md font-bold") API-url
                input-text(v-model="apiPath" @input="onApiUrlChange" placeholder="dripdrop.local")

            div(class="mt-4")
                h3(class="text-md font-bold") Tid
                div(class="flex")
                    p {{ systemTime | dateTime }}
                    button(class="flex flex-col bg-white shadow-md p-2 rounded-lg" @click="onSetTime") Uppdatera tid